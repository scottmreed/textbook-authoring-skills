# Organic chapter review status

**Last updated:** 2026-07-31 
**Canonical order:** `ORGANIC_TEXTBOOK_CHAPTERS` in
`[internal source reference — not in this repo]epoxides` supplement
(counted separately, as it is not one of the 31 catalog chapters).

**No eligible package remains unreviewed.** The four that were outstanding — ch9
`alkynes-organic-synthesis`, ch10 `organohalides`,
ch13 `nuclear-magnetic-resonance-spectroscopy` and ch20
`carboxylic-acids-and-nitriles` — were all reviewed and corrected on 2026-07-31.
The ch18 supplement `epoxides` became eligible on 2026-07-31 when a question set
was authored for it and has also been reviewed.

### Standing follow-ups from the 2026-07-31 batch

- **The recompile-revert cycle is now closed for the whole corpus.** ch9 and ch10
 were the 5th and 6th confirmed instances; both had their curated artifact-only
 values back-ported to `topic.package.json` before compiling. ch13 and ch20 were
 the first two chapters to check CLEAN. Every chapter has now been checked.
- **Three platform gaps were surfaced independently by three or more chapters and
 no chapter edit can clear them:** (1) no renderer reads question
 `structure_smiles`, so items authored as structure-recognition arrive as
 name-recognition; (2) `accessible_description` is consumed by only two
 renderers, so authored non-visual equivalents reach no student on the surfaces
 these chapters use; (3) figure headings skip h2→h4 across the reader.
- **The `BARRIER_HEIGHTS` `high`/`low` coercion bug still has live instances in
 `alkynes-organic-synthesis` (fixed this pass), `carboxylic-acid-derivatives`
 and `epoxides`** — the last two remain outstanding and were identified from ch20's
 review, not from their own.
- **PLATFORM FIXED 2026-07-31 — practice-check callouts printed their own answers.**
 `_practice_check_block` appended "**Answer.** …" to the visible markdown, so every
 self-check was answered before it could be attempted. It now emits the worked answer as
 `content.reveal` + `reveal_label`, and the reader's new `CalloutBlock` renders it behind
 a labelled control that **does not put the answer in the DOM until opened** (so it is not
 readable from the accessibility tree either). Found in manual reader review in ch8's compiled reader;
 it had been recorded on the epoxides supplement and never fixed. **33 packages author
 practice checks — each picks up the reveal on its next recompile, so this is a reason to
 recompile the corpus** (subject to the standing artifact-vs-package diff rule).
 Only ch8 has been recompiled so far.

| Ch | Topic package | Review status | Baseline verdict | Correction status | Last checked |
|---:|---|---|---|---|---|
| 1 | `structure-and-bonding` | ✅ Reviewed | blocked | Verified errors corrected; post-correction estimate **major revision**; full regression review still due | 2026-07-24 |
| 2 | `acids-bases-and-curved-arrows` | ✅ Reviewed | major revision | Verified chemistry, pKa/pH, induction, resonance, donor/acceptor, and assessment-framing errors corrected; post-correction estimate **major revision** — the foundational curved-arrow and comparison assets still compile as unrelated molecule placeholders, and the two-arrow objective remains unassessed | 2026-07-30 |
| 3 | `alkanes-and-stereochemistry` | ✅ Reviewed | blocked | Verified errors corrected same-day (chemistry/reactivity, thermal-barrier explanation, IUPAC key, required-access equivalence, answer leak, invalid torsion tuple, functional-group scope/ester sequencing, matrix labels, Newman descriptions, and five dead links); post-correction estimate **major revision** — core profile specs and the anti-Newman asset still do not satisfy reader rendering contracts, and worked Newman/nomenclature scaffolding plus an ethane retrieval item remain open | 2026-07-30 |
| 4 | `cycloalkanes-and-stereochemistry` | ✅ Reviewed | major revision | Reviewed 2026-07-31 (Instructor 6.6, Struggling Student 4.6, Accessibility 6.8, Visual 3.4 — the lowest visual score in the family). **The first chapter in this family NOT held at `blocked` by the standing structure_scaffold input-path ticket, because ch4 authors no structure_scaffold item** — accessibility raised zero publication blockers and confirmed all nine question types have a keyboard-complete path, so the computed ceiling was `major revision`. Chemically the cleanest early-era chapter: 13/13 distinct SMILES verified by formula, canonical connectivity and InChIKey (including the formula-identical C7H14 and C8H16 traps), 20/20 answer keys re-derived correct, and every prose number survives machine checking (Boltzmann on 7.6 kJ/mol → 95.6:4.4 vs "about 95%"; Eyring on 45 kJ/mol → 8.1×10⁴ s⁻¹ vs "roughly 100,000 times per second"). `expanded` is a true superset of `standard` in all six nuggets, so the ch23/ch30/ch31 default-tier trap does not recur. **Four blockers, all corrected: (1) a verified ring-strain correction existed ONLY in the compiled reader — commits touched the build artifact and never the source, so topic.package.json still taught the error and the next recompile would have deleted the fix** (back-ported and extended to the `standard` tier, which neither file carried); **(2) the chapter's entire subject was undrawn** — chair pucker, axial/equatorial, the ring flip and the 1,3-diaxial contact were depicted nowhere, every figure being the flat polygon the concept's own trouble_spots warns against, and the two assets carrying real geometry both compiled to nothing (`stereochemistry_conversion` has no entry in the reader's `_ASSET_TYPE_TO_BLOCK` so it is silently dropped — first recorded ch25; a conformational profile carries no `steps`/`minima_labels` so no spec inlines and ReactionCoordinateCard falls back to alt text); **(3) both nomenclature short-answer prompts contained their own answers verbatim** ("a cyclohexane ring bearing one methyl group" → methylcyclohexane); **(4) seven level-1 hints stated the graded answer** and the platform's leak guard inspects only `accessible_description`. 14 correction groups applied: four new `diagram` figures built from the diamond-lattice model in `[internal source reference — not in this repo][internal source reference — not in this repo]`, 24 tests) — **all 5 image blocks now resolve, 0 empty**; the cis/trans figure's stereochemistry was cross-checked three ways (CIP, canonical-vs-mirror meso test, methyl-face readout over 8 MMFF conformers), which caught that the build brief had the two SMILES swapped, and the drawn wedge codes are round-tripped through a molblock to confirm they encode the isomers the labels claim; hint bank 24→30 with real ladders; the "three carbons away" 1,3-diaxial miscount fixed (C1→C3 is two carbons); kcal/kJ collision resolved (the numeral 26 named cyclobutane in feedback and cyclopentane in the prose); the energy-profile description corrected from a "twist-boat/boat plateau" to the boat as a local MAXIMUM between two twist-boat minima, with the degenerate low/medium/high buckets replaced by exact fractions; **`long_description` authored for all 13 assets where none of the original 9 had one** (this is a generation-era cohort gap — every package from ch24 on has 100% coverage, every package before it has 0%); the face-vs-orientation independence and the adjacent-carbon cis/trans→axial/equatorial rule promoted from a hint and a distractor into the prose; gauche/steric-strain/Newman/flagpole glossed and the 7.6 kJ/mol penalty derived as 2 × 3.8; **`ch4-chair-tbutyl-v2` rebuilt because its answer was the interface's resting state** (ChairPlacementRenderer pre-selects "equatorial") — now cis-1-tert-butyl-4-methylcyclohexane, where cis-1,4 forces one group axial and the student must choose, verified against the real ChairGrader (correct 1.0, ring-rotated 1.0, swapped 0.0, both-equatorial 0.0, default-accept 0.0); the IUPAC ring-vs-chain convention flagged as superseded. **A recompile-exposed regression was caught and fixed: setting two `wikipedia_title`s revealed the rest were being derived from concept titles and would have shipped two 404s** — all six now explicit, distinct and verified 200. Post-correction estimate **ready with minor revisions**; every remaining high is a platform gap no chapter edit clears (`structure_smiles` renders in no question renderer; `practice_check`/`trouble_spots`/`learning_objectives` reach no reader surface; `accessible_description` is read by one renderer; conformational profiles draw no diagram). 184 tests pass. Not seeded; available:false. | 2026-07-31 |
| 5 | `stereochemistry-at-tetrahedral-centers` | ✅ Reviewed | blocked | Reviewed 2026-07-31 (Instructor 7.0, Struggling Student 4.3, Accessibility 5.2, Visual 4.6). **Chemically the best-verified chapter in the series and the third with zero wrong structures AND zero wrong answer keys** — all 29 SMILES (13 assets, 14 question options, 2 answer keys) match by formula and InChIKey, all 10 CIP descriptors re-derive correctly, all 22 keys re-derived right, and the (R,R)-vs-meso tartaric pair (invisible to a formula check) confirmed both ways: (R,R) not superimposable on its mirror image, meso canonical-SMILES identical to its mirror image. The ch23/30/31 default-tier trap does NOT recur (`expanded` is a self-contained rewrite), and the ch27 dead-wrong-answer-pattern class does NOT recur (16 patterns use the canonical `selected_option_ids` form, both multi_selects use `$contains`/`$missing`). **Six blockers, all corrected.** The headline is a NEW class: **`access-003` is the first structure_scaffold access blocker that is chapter-authored rather than the standing platform ticket** — both drawing items set `typed_structure_entry: "blocked"` and `typedEntryAllowed()` fails closed, so keyboard/switch/voice/screen-reader users had NO input path; **25 of 26 sibling packages set `"allowed"` (only ch1 also blocks)**, and since the answer keys are stereo-tagged SMILES the grader accepts, unblocking restores a gradeable keyboard path. Also: **Fischer projections and D/L are graded twice while 'Fischer' appears 0 times in all three prose tiers, and both drawing items block typed entry because wedge/dash is 'the skill being assessed' while 'wedge' and 'dash' also appear 0 times** — both conventions now taught; 13 hint rungs and 7 `accessible_description`s stated the graded answer (**the compile-time leak guard returns clean on all 22 both before and after, because these are semantic restatements, not answer ids — the guard is not what verifies this class**); and the enantiomer pairs carried interchangeable alt text with 0 of 13 assets having a `long_description` (the pre-ch24 cohort gap) — now 13/13, each deriving its configuration from CIP priorities and sweep direction rather than asserting wedge positions the render may not match. **Orchestrator integrity check found the biggest single defect: ALL SIX Wikipedia links were 404** — no concept authored a `wikipedia_title`, so `[internal source reference — not in this repo]nugget-rs-arrow`, the core scaffold for reading the R/S sweep, compiled LAST — four sections after the CIP rules and behind the advanced prochirality section** — because ch5 alone numbers nugget `order` per-section (1,1,1,1,1,1,2) while the compiler and all 25 siblings treat it as global; renumbered chapter-locally, so zero blast radius. 20 correction groups applied in all, including the `separable_distillation` matrix cell that marked a student wrong for reasoning from the chapter's own non-volatile tartaric acid, specific rotation made operational (relationship, units, and butan-2-ol at ∓13.5 neat — chosen so R is the levorotatory one), 'stereoisomer' defined at last, and `2^n` (voiced 'two caret n') removed. **A persona claim was REJECTED on render evidence:** three parties independently rendered the two priority-arrow figures and confirmed they are true mirror images with correctly opposite sweeps despite identical stereo-free SMILES — the stored-data defect is real but narrower, and was deliberately NOT auto-fixed because adding a chiral tag would draw a wedge that `_orient_conformer_for_config`'s reflection could contradict, turning a correct figure wrong. Post-correction estimate **major revision** — every blocker closed, but two large clusters remain and neither is purely platform: **the chapter's subject is still drawn nowhere** (tetrahedron, mirror plane, superimposability, viewpoint reversal, polarimeter — blocked because `stereochemistry_conversion` is in ALLOWED_ASSET_TYPES but absent from `_ASSET_TYPE_TO_BLOCK`, so it compiles to nothing), and **no question shows a structure to read** (adding `structure_smiles` would change nothing — four renderers read only `imageUrl`). Prochirality carries 3 objectives and 0 questions (documented as a deliberate scoping call). Only 11 of 22 items are surfaced; 11 are staged variants; 0 demo_eligible. 147 tests pass. Not seeded; available:false. | 2026-07-31 |
| 6 | `overview-of-organic-reactions` | ✅ Reviewed | blocked | Reviewed 2026-07-31 (Instructor 5.5, Struggling Student 4.4, Accessibility 5.4, Visual 4.4). **The headline is a regression class, not an authoring class: three verified corrections from commit `[commit ref — not in this repo]` were reverted by a later recompile, reapplied at `[commit ref — not in this repo]`, and reverted AGAIN at `[commit ref — not in this repo]`** — because every one of them was written into the compiled reader only and never back into `topic.package.json`. As found, the chapter was shipping all 6 Wikipedia links as fabricated 404s, `rc-two-step` barriers back to the invalid `high`/`low` (silently coerced to `medium`, so the flagship figure drew two transition states at IDENTICAL height with products at exactly reactant energy — contradicting its own alt text, its prose, its trouble spot and its video brief), and the catalyst sentence back to the imprecise 'consumed and regenerated' in the `expanded` tier the reader renders by default. **Six blockers, all corrected, and all fixed IN THE SOURCE PACKAGE so a recompile can no longer revert them.** Two further blockers were authored: `ch6-classify-reaction-type-v2` stated that heating an alkyl bromide *with a base* gives the alkene 'plus HBr' (with a base present HBr is not a product — the sibling item already had the safe phrasing), and three `accessible_description`s handed screen-reader users what sighted students must derive (two named the product; `ch6-sort-cleavage` supplied the literal grading key as a gloss). **A NEW assessment class: both `rank_order` items authored their cards in EXACTLY the correct order, and `MechanismCardSortRenderer` does not shuffle — it seeds the answer with the presented order on mount, so both items submitted correct without the student touching a control**; this hits keyboard and screen-reader users hardest, who reach them through a Move up/down loop with no cue. Also fixed: all six nuggets were authored `order=1`, so section sequence rested on Python's stable sort rather than the authored key. The recompile finally emitted **12 callout blocks**, so 6 practice checks and 12 trouble spots reach a student for the first time. All 14 SMILES verified correct. Post-correction estimate **major revision** — every blocker closed, but three clusters remain: **the chapter still contains NO figure of any transformation** (no reaction arrow, no curved arrow, no bond cleaving) in a chapter whose own prose calls curved arrows 'the single most transferable skill in the subject', and it grades barb-counting on arrowheads never drawn; **the thermodynamics concept's 3 objectives are assessed by nothing while its only 2 questions rank bond dissociation energies** ('dissociation', 'bond strength' and 'radical stability' occur 0 times in the prose) — left uncorrected because add-the-prose vs move-the-items is a scope call; and neither energy profile has a `long_description`. `functional-group-recognition` and `electronegativity` resolve to no concept in the corpus. 144 tests pass (4 pre-existing failures from uncommitted `GENERATION_METHODS` work, absent at HEAD). 14/14 links now 200. Not seeded; available:false. | 2026-07-31 |
| 7 | `alkenes-structure-and-reactivity` | ✅ Reviewed | blocked | Reviewed 2026-07-31 (Instructor 5.5, Struggling Student 4.5, Accessibility 5.6, Visual 5.2). **The only finding all four personas reached independently, and the inverse of ch6's failure: the chapter READ correctly and was one recompile away from breaking.** All three reaction-coordinate assets specified an `exergonic` first step in the source package — drawing the carbocation and bromonium ion BELOW the reactants, which is chemically wrong and destroys the stability-controls-rate argument the whole chapter rests on. The reader said `endergonic` only because `[commit ref — not in this repo]` hand-patched the artifact; the package and the deck `assets.manifest.json` still held the wrong value, so **the deck surface was already serving the wrong figure today**. The six working Wikipedia links were artifact-only too, with no `wikipedia_title` anywhere in the package. **This is the trap: the chapter NEEDED a recompile (its artifact predates the callout emitter, so 6 practice checks and 6 trouble spots reached no reader) and that recompile was exactly what would have destroyed both fixes.** Package repaired first, then recompiled — now 12 callouts, 6/6 links 200, and all four RC figures endergonic on every surface including the deck manifest. Two answer-leak blockers also cleared: the curved-arrow `accessible_description` stated the arrow's source AND destination (the entire answer to the chapter's only mechanism-drawing item), and the E/Z description stated which geometry is Z, pointing at the key by its own option text. Also fixed: `alk-carbocation-stability-rank`'s description listed cards in ANSWER order not presented order; `alk-sp2-geometry` illustrated ONLY the correct option; and the reversed-arrow wrong-answer pattern matched `lone_pair` at site 1 while site 1 is declared `kinds:['atom']`, so the explanation for the single most common curved-arrow error was dead (the ch27 dead-pattern class, recurring). All SMILES and all specified stereochemistry verified correct by InChIKey and CIP. Post-correction estimate **major revision** — **the E/Z section still has no figure of an E or a Z isomer**: `stereochemistry_conversion` is in `ALLOWED_ASSET_TYPES` but absent from `_ASSET_TYPE_TO_BLOCK`, so `_asset_block` returns None and the asset is dropped silently for EVERY chapter authoring it (same platform gap as ch5/ch25 — fix belongs in `[internal source reference — not in this repo]`). Also open: the hydration profile shows 2 steps where the prose walks 3, omitting the oxonium ion the prose names; the halogenation section asserts stereospecificity while every structure it ships is stereochemically FLAT (RDKit: both centres '?') though the question bank shows correct stereo; the product is drawn as a single enantiomer with no statement that the outcome is racemic; and the Hammond step plus why alkyl groups stabilise a cation exist only inside wrong-answer feedback. Bank is 9 questions for 6 concepts — thinnest non-template package in the corpus. Not seeded; available:false. | 2026-07-31 |
| 8 | `alkenes-reactions-and-synthesis` | ✅ Reviewed | blocked | Reviewed 2026-07-31 (Instructor 5.4, Struggling Student 4.3, Accessibility 4.5, Visual 5.0). **Chemistry structurally sound — all 13 asset SMILES and every structure in all 32 questions machine-verified by formula and InChIKey, every answer key's intended outcome re-derived correct — but two graded items were chemically wrong as depicted:** `ch8-epox-product-v2` keyed a single enantiomer ((2R,3R) trans epoxide) as THE product of epoxidizing achiral (E)-but-2-ene with achiral mCPBA (a racemate; the equally correct (2S,3S) answer was unofferable and the achiral+achiral principle was taught nowhere), and `ch8-diol-syn` labelled ethylene glycol — zero stereocenters — "A syn (cis) 1,2-diol" while its v2 rendered a structure on ONLY the correct option. **The 4th confirmed recompile-revert instance:** `[commit ref — not in this repo]`'s four Wikipedia link fixes and `[commit ref — not in this repo]`'s hydroboration regiochemistry fix lived only in the compiled artifact; the live reader shipped 4/6 links as fabricated 404s and the weaker "boron is electron-deficient" non-sequitur — both back-ported to the package this pass, closing the cycle. **Six blockers, all corrected:** also a blocker-level teach-before-grade gap (anti epoxide ring-opening, "bromonium", 9-BBN, hot-vs-cold KMnO₄, and alkyne hydrogenation each appeared 0 times in any prose tier while being graded — prose now teaches the first four, the alkyne variant rebased to isoprene); **nine `accessible_description`s stated the graded answer** (both draw items literally named the product to draw, both hotspots named the atom, three short answers listed accepted strings, both route items gave the full mapping, one numeric performed the counting — ALL invisible to the compile-time guard because the leaks are semantic); the hotspot items had no neutral non-visual orientation once de-leaked (chapter-side per-atom connectivity readouts added, keyed to the renderer's "C atom N" names after verifying [internal source reference — not in this repo] preserves input-SMILES atom order; renderer naming stays a platform ticket); and **both rank_order items shipped with a grading-pattern issue [redacted](ch6 class) atop whole-bank positional bias — [whole-bank positional-bias pattern — redacted](now positions 2,3,2,2,3,2,3,3,2,1). **The unanimous four-persona finding: a reactions chapter in which no reaction was drawn** — all 13 figures flat isolated structures, five of six nuggets typed `mechanism` with no mechanism shown, and all three syn-stereochemistry claims illustrated on substrates where stereochemistry is invisible (butane; one-stereocenter propene products). **Six `reaction` assets added** (platform type already wired), three with RDKit-verified stereochemistry (1,2-dimethylcyclohexene→cis-1,2-dimethylcyclohexane meso; (E)-but-2-ene→trans epoxide noted racemic; cyclopentene→cis-diol — the naive ring-SMILES stereo guess was caught WRONG by the meso mirror-test: `O[C@H]1CCC[C@@H]1O` is trans, `O[C@H]1CCC[C@H]1O` is the cis meso). 18 correction groups in all: 16 answer-stating hint rungs rewritten procedural; all 8 open-response items gained error-diagnosing feedback via enriched always-firing generic explanations (pattern-matching on those submission shapes risks the ch27 dead-pattern class); two self-answering stems; prerequisites (ozonolysis's sole prereq was catalytic-hydrogenation — no dependency — now functional-groups-overview for aldehyde/ketone recognition); notation normalized across 116 fields; rc-epoxidation long_description carrying the concerted-TS argument; heat of hydrogenation taught in standard tier with real kJ/mol values; opener + consolidation frame; wikipedia_titles all 6 authored and curl-verified (7/7 links 200). **Recompile bonus: 12 callout blocks (6 practice checks + 6 trouble spots) reach a student for the first time** (artifact predated the ch30 emitter). 172 tests pass; compiled-bank audit clean. Post-correction estimate **major revision** — open: intermediate figures (osmate ester, peroxyacid O–O, organoborane, ozonide), platform accessibility delivery (accessible_description reaches no reader surface; hotspot atom naming; h2→h4 heading skip), bank additions needing sign-off (reverse-inference ozonolysis on a fresh substrate; stereochemical hydrogenation product item), video accessibility plans. Concurrent uncommitted `nmr_spectrum` platform work observed in the tree and left untouched. Not seeded; available:false. **SECOND CORRECTION PASS 2026-07-31 — post-correction estimate now `ready with minor revisions`.** Built the four intermediate figures the chapter argued from but never drew (`[internal source reference — not in this repo]`): the cyclic osmate ester, the peroxyacid butterfly transfer, the hydroboration four-centre TS, and the ozonide cleavage map. Each derives its printed claims rather than asserting them — **osmium is deleted in software and the released diol is ASSERTED equal to the chapter's cis (meso) diol and unequal to the trans one**, so a figure that argued for anti addition could not ship; oxygen-transfer bookkeeping is verified by formula subtraction; the carbon labelled "more substituted" is read off the molecule; ketone-vs-aldehyde is decided by H count and the molozonide/ozonide pair asserted isomeric. **All four were rasterised and visually inspected before wiring in, which caught text overrunning the canvas on all four plus two label collisions — none of which a schema check can see**; a bottom-bound assertion now fails the build instead of shipping clipped captions. Reader image blocks 0→4, all resolving. Shared-infra fix: `[internal source reference — not in this repo]#00FF00 # F, Cl` entry while RDKit emits `#00CC00` for Cl — ch8's mCPBA is the first chlorine any figure has drawn, so the guard had never fired. **The two bank additions that needed sign-off were authored and GATED to admin/teacher/contractor preview**: `ch8-ozon-reverse-infer-v2` (reverse inference, cyclohexanone + propanal → propylidenecyclohexane, with a formula-identical C9H16 in-ring trap) and `ch8-hydrog-stereo-product-v2` (1,2-dimethylcyclopentene + H2 → the cis/meso product, S,R, InChIKey distinct from trans S,S). Gating mechanism: `variant_of` a same-type parent keeps them `review_status: draft`, never published, and `build_public_question_summary` drops them from the student projection — verified absent from the compiled student-facing file (16 surfaced, unchanged) and present in the compiled bank; `demo_eligible:false`, tagged `needs-maintainer-review` + `admin-preview-only`. They reach students only if the maintainer promotes them off `variant_of`. Bank 32→34 entries. **THIRD PASS 2026-07-31 (manual review):** the peroxyacid butterfly-TS figure was **WITHDRAWN** — the maintainer found the transition state depicted incorrectly, so the asset, the SVG and its builder function were all removed rather than left to regenerate, with the reason recorded in the builder docstring (re-authoring needs the O–H orientation relative to the breaking O–O bond settled first); the other three figures confirmed good, image blocks 4→3, all resolving. **PLATFORM FIX — practice-check callouts no longer print their own answers**: `_practice_check_block` now emits the worked answer as `content.reveal` + `reveal_label` instead of appending "**Answer.** …" to the visible markdown, and the reader's new `CalloutBlock` renders it behind a labelled control that **does not place the answer in the DOM until opened**, so it cannot be read ahead from the accessibility tree either. This was the standing defect recorded on the epoxides supplement and never fixed; **33 packages author practice checks and each picks it up on its next recompile**. The builder test now asserts the inverse of its old assertion (answer in `reveal`, NOT in `markdown`), plus 2 new renderer tests. 213 python + 84 reader tests pass. Nothing committed. | 2026-07-31 |
| 9 | `alkynes-organic-synthesis` | ✅ Reviewed | blocked | Reviewed 2026-07-31 (Instructor 6.5, Struggling Student 5.4, Accessibility 5.4, Visual 5.2). **The 5th confirmed recompile-revert instance, and the worst flip-flop yet: ch9 was fixed by `[commit ref — not in this repo]`, reverted by `[commit ref — not in this repo]`, restored by the `[commit ref — not in this repo]` merge, and wiped AGAIN by recompile `[commit ref — not in this repo]`** — because every one of those fixes was written into the compiled reader and never into `topic.package.json`. As found, the chapter shipped 7/7 fabricated Wikipedia URLs (all curl-verified 404, all minted by `_concept_wiki_title` falling back to prose titles because no concept authored a `wikipedia_title`) and `rc-hx-addition` back to the invalid `high`/`low` barriers. Every curated artifact-only value was classified and back-ported to the package BEFORE compiling, closing the cycle. **Seven blockers, all corrected.** One outright chemistry error: the anti-Markovnikov hydration route was specified as unhindered **BH₃/THF** in prose and three questions, and BH₃ adds twice across a terminal alkyne — every mapped textbook (McMurry/OpenStax 9.4, Klein, Wade, Smith) specifies a bulky borane precisely to prevent it, so students would have been graded wrong for reproducing the chapter's own reagent; now disiamylborane with the double-addition rationale stated. **Both of the chapter's only two composed figures were broken at render, each contradicting its own alt text:** `rc-hx-addition` authored `barrier: high/low`, which is not in `BARRIER_HEIGHTS` `{small,medium,large}` and is silently coerced to `medium`, so the flagship profile drew **two identical barriers with the product exactly level with the reactants** — no readable rate-determining step and a thermoneutral reaction, in the section whose entire thesis is that step 1 is rate-determining (now `large`/`small` + `magnitude 1.9`: activations 3.6 vs 0.6, product at −1.44, matching the paired question's authored −1.4); and `roadmap-chain-extension` authored its arrows as **`edges` where `SynthesisRoadmapSpec` reads `steps`**, so every arrow rendered blank and the strictly-linear renderer flattened the cis/trans fork into a chain **depicting cis-pent-2-ene converting into trans-pent-2-ene** (rewritten as a linear `steps` route with the trans branch carried as a `scientific_caveat`). **Six `accessible_description`s stated their own answer or method** — both structure_scaffold items named the product class, carbon count and carbonyl position; both hotspots named the target atom; both numeric items supplied the degree-of-unsaturation relation otherwise withheld until hint 3 — and **`find_accessibility_leaks` returns clean on all 28 both before and after**, because the keys are `smiles`/`value` and the leaks spell numbers as words. De-leaking the hotspots would have converted a giveaway into an access blocker (the atom buttons announce only "C atom N"), so both got structured per-atom connectivity readouts keyed to the renderer's 1-based input-SMILES numbering — the ch8 remedy, reused. Also corrected: the Lindlar catalyst was **never named** anywhere in package, reader or bank (6 reagent strings relabelled, composition and exam notation added); the vinyl cation's empty orbital was described as sitting "in an sp framework with high s character", which reads as the vacancy having s character (now identified as an unhybridized p orbital); "an internal alkyne has ... no acidic C-H at all" plus a distractor asserting adjacency never confers acidity (bounded — propargylic pKa ≈ 35, simply out of reach of every base in this chapter); `anti-Markovnikov` existed **only in the `terse` tier** while the graded feedback used it as established vocabulary, and `endergonic` appeared 0 times while a question keys on it; and two concepts declared prerequisites narrower than their prose assumes. **Recompile bonus: 0 → 14 callout blocks**, so 7 practice checks and 14 trouble spots reach a student for the first time. Verified clean by independent orchestrator check: all 31 SMILES correct, **both `rank_order` items are NOT pre-solved** (authored card order ≠ `correct_order`, unlike ch6/ch8), and the compiled bank is byte-identical to the package. 7/7 links now 200; 173 tests pass. Post-correction estimate **major revision** — open: **no alkyne-preparation section at all** in a chapter subtitled "An Introduction to Organic Synthesis" (every synthesis problem must be handed a terminal alkyne); X₂ addition is promised by its concept title, taught in one sentence and assessed by 0 of 28 questions; three stated objectives (why 2°/3° halides fail, choosing a base, terminal-vs-internal from a structure) have no item requiring the decision, and tautomerization — the error the chapter itself calls most common — is never graded at arrow level; **the chapter's spatial claims still have no figure** (the sp axis with two perpendicular π systems, the vinyl cation, the vinyl anion, and one transformation figure per reaction section — the reader supports a `reaction` block and the chapter authors zero); plus platform work no chapter edit clears (rank_order and route-builder renderers discard structures they are already served, `groups` missing from the enrichment key list, `accessibilityBundle` read by only 2 of the renderers used here so 26/28 corrected descriptions still reach no learner, the reader's energy figure on a metered gated route, h2→h4 heading skip). Not seeded; available:false. | 2026-07-31 |
| 10 | `organohalides` | ✅ Reviewed | blocked | Reviewed 2026-07-31 (Instructor 6.0, Struggling Student 5.1, Accessibility 6.0, Visual 4.6). **The 6th confirmed recompile-revert instance:** `[commit ref — not in this repo]` replaced all seven fabricated Wikipedia URLs with real articles in the ARTIFACT ONLY, and the 2026-07-30 recompile `[commit ref — not in this repo]` put all seven 404s back. Targets back-ported to `concepts[].wikipedia_title` before compiling. **Six blockers, all corrected.** Two are chemistry, and both were reached by two personas independently. (1) **The flagship radical-bromination figure loses a carbon:** the asset captioned "the selective bromination product" held 2-bromo-2-methylpropane (`CC(C)(C)Br`, C4) while the substrate beside it — and in the same video storyboard — is 2-methylbutane (C5), so the figure pair depicts a substitution that removes a carbon, and it contradicts the chapter's own `error_repair` key, which correctly says 2-bromo-2-methylbutane. Renamed to `mol-2-bromo-2-methylbutane` with `CCC(C)(C)Br`; both references updated. (2) **The deepest hint teaches a wrong symmetry analysis:** the level-3 `worked_step` on `ch10-monochlorination-count` counts the two methyls on C2 as distinct environments and omits C4 entirely, so it reaches the right answer (4) by an equivalence rule that fails on the next molecule — and on its own v2 twin, whose whole point is that three methyls are equivalent. RDKit `CanonicalRankAtoms('CC(C)CC')` = `[1,4,1,3,0]` confirms atoms 0 and 2 share a class. The other four blockers are answer leaks the compile guard cannot see (keys are `smiles`, and the leaks are product *names*): both `structure_scaffold` items append "The expected answer is …", and both hotspots name the target — v2's "the branch-point carbon" uniquely designates the answer atom. **De-leaking the hotspots would have created an access blocker** (buttons announce only "C atom N"), so both got structured per-atom connectivity readouts verified atom-by-atom against RDKit for `C=CCC` and `C=CC(C)C` — the ch8/ch9 remedy, now three chapters deep. Also corrected: **chlorination was taught as purely statistical** ("in rough proportion to how many of each there are"), which is quantitatively false — replaced with the per-hydrogen weighting 1 : 3.5 : 5 and the worked 64:36 ratio for 2-methylpropane against the 90:10 a head count predicts; **mixed kJ/kcal units** that made an allylic C-H (88 kcal/mol) read as far weaker than a C-Br (285 kJ/mol) when it is stronger — now dual-labelled at all three sites; `3-chloro-2-methylbutane` → `2-chloro-3-methylbutane` (equal locant sets {2,3} → lower locant to the alphabetically first substituent); **five hint rungs that stated the graded ordering, count or assignment**, plus a v2 `region_focus` targeting the single correct atom while its own v1 twin correctly focuses two candidates; the cuprate/water matrix cell was keyed `yes` while the prose said only that copper is "far less basic than a Grignard", arguing the other way — prose now states that less basic ≠ non-basic; the Hammond postulate was invoked by name and never stated; and the tertiary C-H BDE that the section's own conclusion depends on, the halogen scope (F₂ uncontrollable, I₂ endothermic) and the planar-radical racemization were all missing. **`long_description` authored on all 17 assets (was 0/17** — the pre-ch22 cohort gap). **Recompile: 0 → 14 callout blocks**, so 7 practice checks and 14 trouble spots reach a student for the first time. Verified clean independently: all 24 answer keys correct, **both `rank_order` items NOT pre-solved**, and none of the recurring figure hazards apply (no `reaction_coordinate` barrier to mis-coerce, no `synthesis_roadmap` `edges`, no `annotation_font_scale`). 8/8 links now 200; 173 tests pass. Post-correction estimate **major revision** — open: **the chapter's two hardest ideas are drawn nowhere** (no energy profile for the early-vs-late transition state, no delocalized allylic radical, no fishhook arrow anywhere, no R–X → R–MgX polarity flip — all three assigned to unproduced videos); **no transformation is drawn as a transformation** (all 17 assets isolated species; the reader's `reaction` block used zero times); three objectives assessed by nothing (chain steps, reagent choice — SOCl₂ appears in 0 of 24 items — and redox assignment); and platform work no chapter edit clears (**no renderer reads question `structure_smiles`**, so 14 authored structures across six items are dropped and items tagged `representation_tags:["molecule"]` arrive as text lists; `region_focus` ids are zero-based while buttons announce one-based names; h2→h4 heading skip). Deliberately NOT changed: the mixed IUPAC locant conventions and the symmetric homocoupling example — both conventions are valid and rewriting option text risks disturbing answer matching for no chemical gain. Not seeded; available:false. | 2026-07-31 |
| 11 | `alkyl-halide-substitution-and-elimination` | ✅ Reviewed | blocked | Follow-up corrections are not recorded in its review report | 2026-07-24 |
| 12 | `mass-spectrometry-and-infrared-spectroscopy` | ✅ Reviewed | major revision | Verified errors corrected same-day (links, captions, prose gaps, prerequisite, wavenumber); post-correction estimate **major revision** — headline item is the missing spectrum figures (needs a pipeline spectrum asset kind); zero chemistry errors, zero blockers | 2026-07-25 |
| 13 | `nuclear-magnetic-resonance-spectroscopy` | ✅ Reviewed | blocked | Reviewed 2026-07-31 (Instructor 6.4, Struggling Student 6.2, Accessibility 5.2, Visual 6.0). **The best-built chapter of the 2026-07-31 batch and the one with the sharpest defects — and the first with a CLEAN artifact-drift check** ([commit ref — not in this repo]'s edit was already back-ported to the package; `wikipedia_title` authored on all 7 concepts, 8/8 links 200; callouts already emitting). Chemistry verified at a depth no earlier chapter reached: every ¹H/¹³C shift matches literature, every signal count checks against RDKit `CanonicalRankAtoms` across 24 molecules, every multiplicity and integration ratio is right, and all 44 answer keys are chemically correct. **Eight blockers, all corrected.** Two are *adjacent sentences* in the coupling-constants section, both inverted: (1) "**whose outer spacing is the large J**" — in fact each OUTER gap is the SMALL J, the middle gap is J_large − J_small (not a coupling at all), and the large J is the line-1-to-line-3 separation; and (2) "**Unequal intensities … are the tell that you are looking at a dd rather than a quartet**" — backwards, since a dd is 1:1:1:1 and a quartet is 1:3:3:1. The second contradicted the sentence immediately before it, the chapter's own splitting-tree SVG (labelled `dd — 1 : 1 : 1 : 1`), that figure's `long_description`, AND the `ch13-dd-builder` feedback — four internal contradictions and it still shipped. (3) The methyl acrylate spectrum labelled its terminal vinyl protons "trans/cis **to ester**" with the geometry inverted (the 17.3 Hz proton is trans to the internal =CH– and therefore *cis* to the ester), in the one figure whose stated purpose is that a measured J assigns alkene geometry. (4) **A NEW class: four of six `numeric_with_units` items stored `tolerance` as a bare float where `[internal source reference — not in this repo]tolerance.get("mode")`** — confirmed by execution to raise `AttributeError`, so a student answering 7.00 ppm correctly got a server error and the chapter's only quantitative assessment did not run at all. Four more are accessibility: **`ch13-j-readoff` was unanswerable by ANY learner** — its line positions lived only in a `spectrum` config that no surface renders (`numeric_with_units` declares `workspace:"none"`), so the only route to the graded number was a hint that disclosed it; the ¹³C peak-assignment dropdown offered `C1…C5` against a structure drawing carrying no atom numbers (type declares `requires_molecule_description=True`, none supplied); **the integral-reconstruction trace's `aria-label` read out the graded proton counts** — and that leak lives in `frontend/public/assets/nmr/*/asset.json`, *outside* the package, so the compile-time guard never inspects it; and all 44 authored `accessible_description` strings reach no student (consumed by 3 places, none a surface this chapter's 15 types use, and every item is `demo_eligible:false`). 23 correction groups applied, including: J-readoff line positions moved into `prompt_text` and re-spaced so the measurement returns the chapter's own **7.3 Hz** (was 7.5 and 7.2 — three different values for one coupling in a section teaching that partners share one J), the prose slip `0.024 × 300 = 7.3` corrected to 0.0243; **the OH/NH exchange exception** added (n + 1 was taught as universal while the chapter used alcohols in its own examples and hedged around it in `ch13-decoupling-propyl-v2`); the degrees-of-unsaturation formula stated and worked (step 1 of the capstone method was undefined and its declared prerequisite concept does not exist in this package); **anisotropy reconciled** with the opposes-the-applied-field rule two sections earlier (the two statements flatly contradicted each other); the deferred spin-state video marked hidden (it was emitting a live "Watch" link to an empty URL with a storyboard line as its description); and the spin-spin-splitting concept gained its two missing trouble spots — because both nuggets map to one concept slug, the callout rendered **byte-identically twice**, so the coupling-constants section's own traps were never named. Verification: 6/6 numeric items now grade `correct` (was 4/6 raising `AttributeError`); 173 tests pass; guard clean on 44. Post-correction estimate **major revision** — open: **no ¹³C or DEPT spectrum anywhere** (the up/down/absent phasing is a visual fact prose cannot carry, and the question bank ships the ¹³C spectrum the chapter body never shows); **every peak and region label authored on the four spectra is dropped by both renderers** (`GenericSpectrumCanvas` never renders `r.label`), so *the sighted reader currently gets less than the screen-reader user* — a strong `long_description` layer is masking the render defect; methyl acrylate still cannot resolve its 1.5 Hz geminal coupling at 300 MHz over a 4 ppm window (the renderer's fixed 0.008 ppm linewidth is a hard floor); three objectives assessed by nothing (DEPT classification, selective decoupling, structure construction — the capstone is a 50% pick between two named isomers); and `accessible_description` reaching no student, which is why the six corrected descriptions cannot be verified in-product. Not seeded; available:false. | 2026-07-31 |
| 14 | `conjugated-compounds-and-ultraviolet-spectroscopy` | ✅ Reviewed | ready with minor revisions | Verified errors corrected same-day (resonance-form label, stereodescriptor locant, energy-profile accessible descriptions, notation gloss, constant gloss, kinetic-control rationale, endo scoping, numbering mapping); post-correction estimate **ready with minor revisions** — open items are the visual-scaffolding cluster (MO ladder, kinetic/thermo branch profile pending RC-policy call, allylic + Diels–Alder sequences) and platform keyboard-input verification; zero chemistry errors, zero blockers | 2026-07-27 |
| 15 | `benzene-and-aromaticity` | ✅ Reviewed | blocked | Verified errors corrected same-day, 21 changes (inverted ortho-isomer-count evidence, "Structure A–D" option labels, illustrated-option answer tell in 10 items where 9 of 10 illustrated options were the key, acidity ranking contradicting the chapter's own cycloheptatriene claim, ¹³C symmetry condition, toluene IR band count + missing meta ranges, pyrrole pKa 0.4→−3.8, cyclobutadiene square/rectangle contradiction, imidazolium pKa, prerequisites, 8 alt texts, hydrogen-render consistency, classification-card structures, IR descriptions, hotspot ring numbering, video accessibility requirements, Frost-circle name, anthracene reasoning, and a Wikipedia link that pointed at the geophysics "Ring current" article); post-correction estimate **blocked** — chemistry and assessment-validity blockers are all resolved but one required-access blocker is unresolved (structure_scaffold accepts only canvas drawing; accessibility_bundle reaches no renderer), plus the visual-scaffolding cluster (MO ladder / Frost construction, lone-pair geometry, hydrogenation energy comparison, ring-current field); zero wrong answer keys. **Seeded bank is now stale — 20 of 46 items changed; reseeding needs permission.** | 2026-07-28 |
| 16 | `electrophilic-aromatic-substitution` | ✅ Reviewed | blocked | Verified errors corrected same-day and re-seeded to prod (un-enterable multi-position hotspots rewritten, lettered options made non-visually distinguishable, accessible KMnO₄ item pair added, free-Br⁺ labeling unified, free-primary-carbocation and aniline-caveat chemistry framing fixed, hint/key contradiction, o/m/p gloss + naming prerequisite, honest figure alts, oxocarbenium resonance asset, dead link); post-correction estimate **ready with minor revisions** — open items are platform accessibility delivery (accessibility_bundle, hotspot labels, Ketcher keyboard path, reader practice checks) and the visual-scaffolding cluster (arenium comparison panel, electron-flow sequence, classification chart); zero wrong answer keys | 2026-07-28 |
| 17 | `alcohols-and-phenols` | ✅ Reviewed | blocked | Verified errors corrected same-day (9 changes): SOCl₂ stereochemistry (was wrongly shown as inversion beside PBr₃ — now PBr₃ SN2 inversion vs SOCl₂ SNi net retention/pyridine-inversion), free-primary-carbocation model removed, two structure_scaffold accessible_descriptions de-leaked (no longer name product class), pKa log-scale bridge added, carbinol-carbon defined explicitly, "1°/2°/3° does not apply to phenols" stated, induction-vs-resonance paragraph split, "tetrahedral intermediate" glossed, reagent formulas subscripted. Post-correction estimate **blocked** — sole remaining blocker is the platform-wide structure_scaffold keyboard/non-drawing-input limitation (access-001, same as ch1/11/15/16); zero wrong answer keys, all chapter-authored chemistry/answer-leak blockers resolved. Not seeded; available:false. | 2026-07-28 |
| 18 | `ethers-epoxides-thiols-and-sulfides` | ✅ Reviewed | blocked | Verified errors corrected same-day and pushed to the seeded prod bank (12 groups): cis-2,3-dimethyloxirane SMILES actually encoded the trans (R,R) epoxide → corrected to meso, RCR accessible_descriptions gained the neutral plotted-data readout non-visual learners need (pushed to DB directly — seeder hash ignores bundle-only changes), boiling-point rankings regraded against literature (pentane boils ABOVE diethyl ether — cards swapped to butane/pentane triples), impossible-locant distractor name fixed, Hammond postulate now taught before being graded, bromohydrin figure swapped to stereodefined trans-2-bromocyclohexanol, regio product figures co-located, cis-2-butene before/after added, jargon glosses (disconnection, Claisen enrichment framing, elision/HSAB), video color-only carrier requirement recorded. Post-correction estimate **ready with minor revisions** — remaining blocker is the platform structure_scaffold input-path ticket (access-002, same as ch1/11/15/16/17) + widget keyboard paths; open recommendations: deferred regio video, crown host-guest + sulfur mechanism assessment coverage. One wrong-structure asset was the only chemistry error; all 38 answer keys verified correct. Seeded + available:true; corrected rows live. | 2026-07-28 |
| 18 supplement | `epoxides` | ✅ Reviewed | major revision | Question set **authored, seeded and reviewed 2026-07-31** — the package previously had 6 concepts, 6 nuggets and 5 assets but **no `question_sets` key at all**, which is why it had been ineligible. 16 items (8 surfaced + 8 staged `-v2`) across 8 types, deliberately differentiated from live ch18's existing 22 epoxide questions by substrate (1,2-epoxybutane, 2,2-dimethyloxirane, cyclohexene oxide, cis-2,3-dimethyloxirane) and by using 4 types ch18 never used for epoxides. Reviewed same day (Instructor 5.6, Struggling Student 4.3, Accessibility 6.8, Visual 4.4). **Core chemistry machine-verified correct**: 2,2-dimethyloxirane gives 1-methoxy-2-methylpropan-2-ol under base and 2-methoxy-2-methylpropan-1-ol under acid (formula-identical, distinct InChIKeys, correctly assigned); cis-2,3-dimethyloxirane is meso; cyclohexene oxide + hydroxide gives the trans diol; acid sequence protonate→attack→deprotonate. **Two blockers, both corrected: (1) a wrong answer key was live** — `epox-two-routes-matrix-v2` asserted halohydrin cyclization does not carry substituent geometry into the product, false for an intramolecular Williamson SN2 with inversion at the C–X carbon, and contradicting both this chapter's prose and live ch18 (row relabelled to name the C=C sense, feedback rewritten to affirm the closure IS stereospecific); **(2) all 8 per-option distractor explanations were dead** — authored under `feedback_bundle.explanations` while the engine reads `wrong_answer_explanations`, so the engine saw 0 of 8 and students got only the generic fallback. **epoxides was the only one of 32 packages with that defect** (1767 entries under the correct key elsewhere); found by the struggling-student persona, not by the authoring harness, which had verified `_pattern_matches` in isolation rather than through `_explanation_fixture`. 10 correction groups: also six 404 Wikipedia links (the ch4 defect recurring — compiler slugifies the concept title when no `wikipedia_title` is set; six distinct titles now curl-verified 200), seven answer-giving hint rungs (both rank_order ladders pinned first and last of three cards, fixing the whole ordering), both energy profiles rewritten from the `{points}` shape the reader ignores into `steps`+`minima_labels` so they now render (the acid profile is genuinely 3-step/4-minima, where the old 4-point curve had one maximum and contradicted its own "two-stage" alt text), long_descriptions for all 5 assets, the unassessed acid regiochemistry surfaced (previously only the staged variant carried the acid answer while the basic rule was graded 3×), partial credit on 10 multi-part items, widened short-answer phrasings, and four item-construction defects. Post-correction estimate **ready with minor revisions**. Remaining are mostly platform: `stereochemistry_conversion` still reaches no reader (the ch25 gap, root-caused on ch4) so the backside-attack figure is invisible; the bank is still entirely text because no question renderer reads `structure_smiles`; practice_check callouts print their own answers. **Seeded (owner 1) and available:true** — note the chapter went available before the review ran, and `science_review.status` is still `not_reviewed`. | 2026-07-31 |
| 19 | `aldehydes-and-ketones` | ✅ Reviewed | blocked | Verified errors corrected same-day (12 groups): the chapter's lone chemistry error — "common hydride reagents add 1,2 to enones," false for NaBH₄ (Luche conditions exist for a reason) — rewritten with LiAlH₄-vs-NaBH₄ specifics; gem-diol glossed at first use; alpha/beta, 1,4 counting origin, enol/enolate/tautomerization, soft-nucleophile, and kinetic-vs-thermodynamic control defined at first use; pH-4.5 optimum re-grounded in real speciation (pKaH ≈ 10–11, rate-product framing); both IUPAC items made text-only (key and distractor had byte-identical structure renders); racemic outcome stated where the stereocenter forms; Bürgi–Dunitz named and corrected to ~107°; Baeyer–Villiger exception added to the ketone-inertness absolute; drawing-item level-3 hints no longer contain the answer SMILES; error_repair stimuli described non-visually; MVK alt text maps locants to alpha/beta; six wikipedia_titles underscored (compiled links had raw spaces). Post-correction estimate **blocked** — sole remaining blocker is the platform structure_scaffold input-path ticket (same as ch1/11/15/16/17/18); setting it aside, estimated ready-with-minor. Open clusters: visual scaffolding (rec-008, same as ch14–18), assessment coverage (IR/¹³C, ozonolysis, NaBH₄-vs-LiAlH₄), practice_check reader delivery, accessible_description delivery (PROVED on ch16). Zero wrong answer keys; zero accessibility leaks. | 2026-07-29 |
| 20 | `carboxylic-acids-and-nitriles` | ✅ Reviewed | major revision | Reviewed 2026-07-31 (Instructor 6.3, Struggling Student 6.6, Accessibility 7.6, Visual 6.0). **The strongest chapter of the 2026-07-31 batch and the only one of the four NOT computed `blocked`** — because it is the only one with no unresolved required-access blocker. Accessibility raised **zero** blockers (7.6, the highest single score in the batch): all four `structure_scaffold` items set `typed_structure_entry:"allowed"`, nothing is gated on drag/hover/colour/motion, **no `accessible_description` leaks an answer and no level-1 hint discloses a key** — the standard the other three chapters all failed. Artifact-drift check CLEAN ([commit ref — not in this repo]'s edit already back-ported, byte-verified), `wikipedia_title` on all 9 concepts, 10/10 links 200, callouts already emitting. The quantitative layer is exceptionally reliable: every pKa, boiling point, bond length (formate 127 pm vs 120/134), IR wavenumber and NMR shift checks against standard tables, and all 22 asset SMILES plus every structure across 42 items parse in RDKit and match their names — including the charged species and both para-substituted benzoic acids. **Four blockers, all corrected, all wrong chemistry reaching a student.** (1) Section 1 taught that delocalization "**weakens the O–H bond**" and makes acids acidic — false (the carboxyl O–H BDE is *higher* than an alcohol's, and stabilizing the neutral acid opposes ionization), and it is the exact misconception the chapter's own trouble spot names and section 3 explicitly refutes ("the O–H bonds involved are similar in strength"). (2) **`ch20-stronger-than-acetic` keyed 4-methoxybenzoic acid (pKa 4.47) as NOT stronger than acetic acid (4.76)** — a number the chapter prints in two places — so a student reasoning correctly from the chapter's own table was graded wrong, and the distractor rationale hid it by silently switching the reference compound from acetic to benzoic acid. Option replaced with phenol (pKa 10); key `['a','b','e']` now correct. (3) The preparation practice check called **neopentyl bromide** "primary and unhindered at the reacting carbon" with cyanide displacement "straightforward" — it is the canonical primary halide that does *not* do SN2 (~10⁻⁵ the rate of ethyl bromide); now resolves to Grignard carboxylation, with the β-branching caveat added to the prose. (4) The nitrile-IR feedback and its L2 hint taught the nitrile band sits "**above the alkyne range**", contradicting the chapter's own 2100–2260 cm⁻¹ window — corrected to the discriminator the chapter actually teaches (intensity plus the ≡C–H stretch). 22 correction groups in all, including: **pKa defined at last** (used 49 times in the rendered reader, never defined; the only direction statement sat in a callout three sections after the first comparison); **"oxidation level" defined** (carried the graded feedback of 9 questions, 16 occurrences, and appeared once undefined in the whole chapter); the acetate figure's alt text rewritten plus a new `long_description`, because **its description restated the non-equivalent bonds the adjacent callout forbids** — the misconception was reaching the non-visual channel too; all three deferred videos marked hidden (they were rendering live "Watch" links to empty URLs with **producer camera directions as the student-facing description**); both IR tolerances narrowed 20→10 cm⁻¹ because ±20 *is* the conjugation shift, so each item accepted the very value its prompt ruled out (verified: keys still correct, conjugated values now incorrect); the three-way solubility threshold resolved; DIBAH bound to its full name; and the cyanide nitrogen added as a selectable site so `ch20-nitrile-sn2-arrow` can diagnose the ambidentate misconception its own feedback addresses. Verified clean independently: **no `reaction_coordinate` asset at all, so the `BARRIER_HEIGHTS` coercion bug has no instance here** (the live `high`/`low` instances are in `carboxylic-acid-derivatives` and `epoxides`), the one `synthesis_roadmap` matches the `{nodes, steps}` contract exactly with `is_target` set — the counter-example to ch9's broken roadmap — and every selected-response item is all-or-none on option structures. 173 tests pass; guard clean on 42. Post-correction estimate **ready with minor revisions** — the only chapter in this batch to clear `major revision`. Open: **19 of 20 assets are single molecules, so the cyclic dimer, the carboxylate hybrid and an IR band shape cannot be drawn at all** (and the chapter grades curved arrows and bond-change ledgers on electron flow it never draws); platform gaps now surfaced by three consecutive chapters (no renderer reads question `structure_smiles`, so six items authored as structure-recognition arrive as name-recognition; `accessible_description` inert; images-off drops the figure's teaching sentence; h2→h4 skip); extraction/amide-dehydration/nitrile-nomenclature objectives assessed by nothing; five `-v2` items registered `variant_of` while testing a *different* sub-skill. **NOT corrected, with the persona's mechanism corrected:** the duplicated extraction callout comes from `nugget.concept_slugs`, not `section_id`, so extraction-specific traps would surface in the acidity section too — the clean fix is a separate concept, which is structural. Not seeded; available:false. | 2026-07-31 |
| 21 | `carboxylic-acid-derivatives` | ✅ Reviewed | blocked | Verified errors corrected same-day (4 groups): the chapter's lone chemistry error — a Grignard adds twice "to that more reactive ketone", which inverts the chapter's own reactivity ladder and was additionally circular (the ketone IS the acid chloride's substitution product) — rewritten in both tiers with the correct reason plus an explicit note that the ladder ranks only the heteroatom-substituted derivatives; both lactone/lactam recognition items had a different compound drawn on every option, so the key was selectable by matching the picture to the stem — all options now show the subject compound; the acid-to-amide `synthesis_roadmap` (flagged independently by ALL FOUR personas) compiled to a *visible* empty-URL image block, unlike the deferred videos which compile hidden — replaced with a benzoic acid → benzoyl chloride → N-methylbenzamide molecule sequence with its reagent rationale and caveats moved into prose (the ch23 substitution pattern); both energy-profile accessible_descriptions opened "A two-maximum energy profile", leaking the step count the item asks for — rewritten to give the plotted height of every stationary point and perform no comparison. Post-correction estimate **major revision** — all chapter-local blockers resolved; remaining are the three platform access blockers (accessible_description reaches no renderer; ReactionCoordinateQuestionRenderer reads `description` not `accessible_description`; structure_scaffold blank canvas has no non-pointer path — same as ch1/11/15-19/22). Instructor RDKit-verified all 21 asset SMILES and all 48 answer keys: zero wrong keys. Recorded not applied: leaving-group-rule/saponification reconciliation, IR frequency-vs-reactivity counterexample, and the nitrile family promised in the title but never taught or assessed. **Not seeded; available:false.** | 2026-07-30 |
| 22 | `carbonyl-alpha-substitution` | ✅ Reviewed | major revision | Verified errors corrected same-day (13 groups, 22 edits): the sole blocker — both reaction_coordinate_reasoning items leaked step count (accessible description said "two-step") and rate-determining step (prompt said "slow enolization, then fast capture") — de-leaked with neutral geometric narration carrying relative heights for non-visual learners; homoallylic-mislabeled distractor, carboxylate-equivalence overstatement, enolate forward reference, haloform prerequisites, "three stages"-vs-two-step profile inconsistency, transformation-first expanded-tier openings, placement-neutral acetophenone caption, concept-1 sorter's premature enolate items swapped for prior-chapter resonance pairs, hint string-match, "in order" ledger prompt, orientation-dependent alt text, open-chain H-bond caption, "soft" gloss, one-arrow scope notes; added the kinetic + thermodynamic enolate figures of 2-methylcyclohexanone and two question pairs (enol-content ranking; kinetic-alkylation product), bank now 54 items / 15 types. Post-correction estimate **ready with minor revisions** — remaining highs are all platform-level (roadmap empty-URL rendering + practice_check/long_description emission, both verified identical in ch21; structure_scaffold input-path ticket, same as ch1/11/15-19; profile-widget AT exposure). Zero chemistry errors (instructor RDKit-verified all ~60 structures and 50 keys); zero wrong answer keys. | 2026-07-29 |
| 23 | `carbonyl-condensation` | ✅ Reviewed | blocked | Produced and reviewed 2026-07-29/30. Verified errors corrected same-day (11 groups): the chapter twice claimed **octane-2,7-dione closes to a seven-membered ring** when the C3 enolate reaches C7 across only five atoms and the five-membered closure wins (product is 1-acetyl-2-methylcyclopent-1-ene) — this passed every automated check because both candidates are C8H12O isomers, and it contradicted the chapter's own strain-free-closure principle while leaving the matching item with no correct option; item re-based on four unambiguous substrates, practice_check re-based on nonane-3,7-dione, and the missing generalisation (count from EVERY alpha carbon and take the smallest unstrained ring, not from the chain ends) added using octane-2,7-dione itself as the worked illustration. Also: the capstone Robinson worked example enumerated five ring atoms and called them six (omitting the side-chain ketone carbon that becomes the new enone carbonyl); acid-catalyzed dehydration had been written as a free **secondary** carbocation E1 — an error introduced by production's own coherence pass while closing a different gap — corrected to the enol route in prose and in the comparison-matrix item; an impossible distractor name ("4-methylpentane-2,4-dione") over a pentane-2,4-dione structure replaced with a true isomer; four definitions (condensation, aldol, addition-vs-condensation naming, annulation) existed ONLY in the `standard` tier while the reader defaults to `expanded` — all four added; mixed-aldol product count reconciled at four-to-two; acidity-ranking variant re-based on values the chapter actually supplies; both categorize items given structures; retrosynthesis item given the target it references; both profile descriptions tightened to plotted heights with no comparison performed. Post-correction estimate **major revision** — all chapter-local blockers resolved; remaining are the same three platform access blockers. **The ch22 answer-leak class was cleanly avoided at baseline** (accessibility persona verified both profile descriptions leak-free). Zero synthesis_roadmap assets authored by design (ch22 proved they render empty). **Not seeded; available:false.** | 2026-07-30 |
| 24 | `amines-and-heterocycles` | ✅ Reviewed | blocked | **APPROVED by maintainer 2026-07-30: available:true** (Pending badge cleared); still unseeded. Produced and reviewed 2026-07-30 (same day). **Zero chemistry errors and zero wrong answer keys at baseline** — the instructor persona verified all 26 asset SMILES and all 84 question structures with RDKit against formula, canonical connectivity AND InChIKey, re-derived all 56 keys, and checked ~20 pKa values; the formula-identical-isomer trap that caught ch18 and ch23 did not recur (azo dye E geometry, 2,4- vs 2,5-dinitroaniline, histamine C4, quinoline vs isoquinoline all correct). The ch22 answer-leak class also did not recur (both RCR descriptions leak-free; automated guard clean on all 56). Verified errors corrected same-day (21 groups): the two blockers were **accessibility-channel-only chemistry errors** — both Hofmann drawing items called a primary amine "secondary" (teaching non-visual learners ONLY the exact misconception concept 1 exists to destroy, and contradicting a sibling item whose 3-equiv key depends on it), and ch24-hofmann-equivalents-v2 stated the "cyclic tertiary" classification that IS its answer while its v1 twin was written correctly. Also: **production's coherence pass introduced an error again** ("two rings, twice the discount" refuted by the 6.1-then-3.8 increments printed beside it) — same pattern as ch23, now twice-confirmed; halogens graded twice but taught nowhere while the stated rule (reuse EAS directing vocabulary) predicts the WRONG SIGN for chlorine; the two Hofmann reactions (rearrangement vs elimination) never disambiguated; Hofmann rule stated without its E1cb/styrene exception; six text defects (truncated "C3, C5...", "three reducing systems" listing two, hundred-million- for billion-fold, "Neutral NH2-" for an anion, false industrial-phenol claim, PLP transamination conflated with reductive amination); ASCII prose vs Unicode prompts normalised across 45 fields (screen readers said "C hash N"); 6 descriptions promised drag-and-drop the UI does not ship; show_hydrogens applied inversely to teaching load; ledger labelled two atoms absent from its own stimulus; curved-arrow level-3 hints read out the key; prerequisites mismatched prose dependencies. **NEW platform finding: rank_order cannot show structures** — the backend enriches `cards` with imageUrl but MechanismCardSortRenderer has no imageUrl path, so authored structures are silently dropped (structures added only to categorize_groups and comparison_matrix, which verifiably render them). Post-correction estimate **blocked** — sole remaining blocker is the platform structure_scaffold input-path ticket (same as ch1/11/15-19/23); **setting it aside, estimated ready-with-minor**. Open: pKaH-ladder / diazonium-menu / pyrrole-vs-pyridine-orbital visuals (all 26 figures are static single molecules), no amine nomenclature taught, azide/Gabriel objective assessed only through failure cases. profiles-clobber hits 14-15. **Not seeded; available:false.** | 2026-07-30 |
| 25 | `biomolecules-carbohydrates` | ✅ Reviewed | blocked | Produced and reviewed 2026-07-30 (same day). Instructor machine-verified all 30 asset SMILES, all 34 distinct question structures and every numeric claim, and hydrolysed all four disaccharides in silico — 29 of 30 structures correct, including the dual-anomeric sucrose linkage. **The one wrong structure is a big one: `mol-beta-d-xylopyranose` shipped the ALPHA anomer**, disproving the caption, the prose and a question key that all rest on it being all-equatorial — and the source of the error is that `backend/app/data/sugar_forms.json` HAS ALPHA AND BETA SWAPPED FOR ALL FOUR D-ALDOPENTOPYRANOSE PAIRS (xylo, lyxo, ribo, arabino). That is a Ring Closure Tutor data bug, left unfixed here because it is outside the chapter — **flag for maintainer**. Verified errors corrected same-day (18 change groups): xylopyranose anomer; a comparison_matrix cell keying 'free glucose survives 1 M NaOH' against the chapter's own Lobry de Bruyn–van Ekenstein passage; **both curved_arrow items ungradable for everyone** (renderer emits `site.kinds[0]`, so `['atom','lone_pair']` could never match a lone_pair key — ch24's `['lone_pair']` form is the fix); **both chair items scoring 1.0 for a single placed substituent** (expected_label grades the SET of orientations); the ch15 illustrated-option answer tell recurring in 4 items; 17 alt texts that did not distinguish the figures they ask you to compare; whole-bank positional bias ([whole-bank positional-bias pattern — redacted], all matching diagonal, both rank_order pre-sorted); an anomeric CIP wrong in an accessibility-only field; the 'humans secrete no β-glucosidase at all' absolute the chapter refutes itself with lactase; a flat mutarotation profile drawing both anomers isoenergetic; graded evidence that existed in NO tier; and feedback keyed to a CORRECT option. Post-correction estimate **blocked** — every chapter-authored blocker is resolved and machine-verified, and the two that remain are platform: hotspot atoms named only by RDKit index (sugar C1 announces as 'C atom 5'), and the structure_scaffold keyboard ticket already open on ch1/11/15/16/17/18/19. Setting those aside, estimated **ready with minor revisions**. Headline open item: the chapter teaches Fischer, Haworth and chair and **draws none of them** — all four personas found this independently; needs a projection asset kind the schema lacks. **APPROVED by maintainer 2026-07-30: available:true** (Pending badge cleared); still NOT seeded, so the reader homework preview stays empty until a seed run is approved. Tracing this chapter's wrong structure also uncovered and FIXED two anomer-label bugs in `backend/app/data/sugar_forms.json` — all four D-aldopentopyranose pairs AND all four D-ketopyranose pairs had α/β swapped (the latter found by a D/L enantiomer-consistency check; L was right, D was wrong). All 128 forms now verify. | 2026-07-30 |
| 26 | `biomolecules-amino-acids-peptides-and-proteins` | ✅ Reviewed | blocked | Produced and reviewed 2026-07-30 (same day). **Chemically the cleanest chapter in the series so far** — the instructor persona machine-verified all 32 asset SMILES and all 42 distinct structures inside questions, every stereodescriptor, every net formal charge, every pI recomputation and all 40 answer keys, and found **zero wrong structures and zero wrong answer keys**. The L/D trap this package type is most prone to did not recur: all 18 chiral L-amino acids (S), cysteine (R), threonine (2S,3R), isoleucine (2S,3S), both cystine halves (R). All six PDB claims check out against RCSB, including that 1ZNI assembly 1 is the A+B heterodimer the alt text describes and 1HHO assembly 1 is the α₂β₂ tetramer. Both baseline blockers were single clauses and are corrected: the L-alanine figure generalised the (S) result to 'every L-amino acid whose side chain begins with carbon' (cysteine's does, and it is R), and the Edman walkthrough added the amine across C=S rather than C=N, contradicting its own next step. Verified errors corrected same-day (17 change groups): glycine pKa unified chapter-wide on 2.34/9.60 (pI 5.97 — the questions had 2.35/9.78/6.06 while the prose ran its Henderson-Hasselbalch worked example on 9.60); five-family prose reconciled with the four-bin assessment taxonomy that keys tyrosine polar-uncharged; **asset↔nugget links reconciled in BOTH directions and made derived** (14 assets claimed a nugget that never rendered them — including histidine claiming the enzyme section it argues from — and 16 rendered in sections their metadata did not claim, which was the stale-caption mechanism); neutral free-base structures moved out of the charge-ladder section (they depict species existing at no pH); lysozyme and citrate synthase alt text no longer promise a groove/cavity their apo ribbon renders do not show; myoglobin alt text no longer asks readers to count eight helices a rainbow ramp cannot separate; proline exceptions added to all three protease specificity rules plus the blocked-N-terminus Edman failure mode; oxyanion hole and Fmoc UV absorbance taught (both were graded but absent); the 40%-double-bond 'implying' inference, the pyruvic-acid Strecker contrast, and 'free' phi/psi rotation all corrected; five bare chapter-number cross-references replaced with topic names (they collided with this package's own Klein-25 and Fundamentals-23 mappings). **NEW platform findings: the compiler dropped every `long_description` on the way to the reader** (now passed through and rendered — 68 blocks), **and protein image blocks carried no `content.attribution`, which is the only field the reader renders**, so RCSB credit reached nobody. Post-correction estimate **blocked** — all three remaining blockers are platform accessibility delivery (accessible_description rendered by no surface, hotspot atoms named only 'C atom 4', structure_scaffold has no non-drawing path — same tickets as ch1/11/15-19/23/25); **setting those aside, estimated ready-with-minor**. **The titration curve — the single most-cited gap, found independently by all three non-instructor personas — has since been BUILT**: a new `titration_curve` asset kind plus exact alanine and lysine curves computed from the charge balance, pKa values confirmed against PubChem's curated Dissociation Constants (alanine 2.34/9.69 Kirk-Othmer; lysine 2.18/8.95/10.53 Merck Index). Open: three remaining told-not-shown figures (catalysis energy profile, residue-scale helix/sheet, planar peptide unit), and two objective-alignment assessment gaps (cysteine R/S and the Strecker carbon count are never assessed) left unapplied because bank expansion needs sign-off. Not seeded; available:false. | 2026-07-30 |
| 27 | `biomolecules-lipids` | ✅ Reviewed | blocked | Produced and reviewed 2026-07-30 (same day). Prose and numbers are the best-verified in the series — the instructor persona checked every melting point against the literature (lauric 44, myristic 54, palmitic 63, stearic 70, oleic 13, elaidic 45, linoleic −5, α-linolenic −11, arachidonic −49), every derived gap, cholesterol's 17+2+8=27 carbon accounting and the prostaglandin's 7+8+5=20, and **34 of 36 SMILES match authentic InChIKeys**. **Two chemistry blockers, both corrected:** `mol-pge2` shipped the C15 epimer — 15-epi-PGE2 (XEYBRNLFEZDVAW-**GKEZHNTD**SA-N) rather than PGE2 (…-**ARSRFYAS**SA-N), rendered in two reader sections and the substrate of three questions, and **invisible to a formula check because both epimers are C20H32O5**; and `ch27-steroid-rigidity-error-repair-v2` graded as correct a repair asserting terpenoids and steroids are *not* built from acetyl units, which is false (all 27 cholesterol carbons trace to acetyl-CoA via HMG-CoA/mevalonate — the chapter's own terpenoid nugget says so) — re-scoped to the true distinction, C2 assembly unit vs C5 assembly unit, same ultimate source. **A third blocker made two questions ungradeable for everyone:** both `newman` items declared `principal_labels: ['CH₂R','CH₂R']` while the butane preset only ever supplies `CH3`, so `_principal()` returned the ambiguous sentinel, `classify_newman` returned None and the grader escalated to teacher review that self-study never provides — and the v2 declared `expected_class [redacted]`, a class the classifier cannot emit at all, so a correct answer scored wrong. **Orchestrator integrity check found a fourth, chapter-wide: 91 wrong-answer `match` patterns across 32 questions were unreachable** — `explanations._pattern_matches` subset-matches against the *submission*, so the `{'option_id':'b'}` authoring form never fires and every distractor-specific explanation silently fell back to the generic one (0 fired on a correct submission, so no correct student was mismarked). Verified errors corrected same-day (21 change groups): the four above, plus `'Structure 1'–'Structure 4'` option labels that made two items unanswerable without sight (the ch15/ch16 lettered-option trap recurring), the collapse-step curved-arrow question showing the *pre-attack* structure while its labels described the tetrahedral intermediate, a chair hint teaching the 1,3-diaxial stability comparison the nugget explicitly disqualifies for a locked ring, four `structured_reasoning` text fields worded as prose but graded by whole-string membership in a 3-word vocabulary, four questions tagged to concepts 3–5 sections before their content, `mol-limonene` drawn as (S) while captioned as the citrus compound ((R)-(+)), four figure descriptions asserting labels/markings the renders do not carry, both evidence boards having a uniform key and a never-correct 'Neutral', omega-3 graded at standard difficulty though present in no objective and only the expanded tier, two accessible descriptions handing over the reasoning step, both variants drifting off their parent's objective, ASCII/Unicode notation splitting the chapter at section 6, an exceptionless 'every eicosanoid' rule (series-1/-3 and cysteinyl leukotrienes), and three `wikipedia_title` values carrying raw spaces. Post-correction estimate **major revision** — all five chapter-side blockers are closed and machine-verified (InChIKey, CIP, `classify_newman` round-trip, 0/0 pattern audit, 319 tests green); it does not drop to ready-with-minor because **all four video briefs were deferred to nothing**, leaving the chapter's four geometric arguments (cis-kink packing, the saponification mechanism, micelle-vs-bilayer, the trans-decalin flip) with **no figure of any kind** while each retains a question that assumes one — every non-instructor persona found this independently. Remaining blocker is the platform structure_scaffold input-path ticket (same as ch1/11/15–19/23/25/26/28); hotspot atom naming is partially mitigated chapter-side. **A persona recommendation was rejected on platform evidence:** the visual persona asked for structures on the `rank_order` cards, but that renderer drops `structure_smiles`, so it would have changed nothing on screen — retained as an open need against the renderer. Not seeded; available:false. | 2026-07-30 |
| 28 | `biomolecules-nucleic-acids` | ✅ Reviewed | blocked | Produced and reviewed 2026-07-30 (same day). Instructor persona machine-verified all 20 asset SMILES plus the structures inside questions: every InChIKey matches the authentic compound, every nucleoside is beta at C1', the dinucleotide really is dA(3'→5')dT with the right free ends, and **both hotspot answer keys point at the correct atom** (atom_15 = ATP's alpha phosphorus; atom_7 = the free 3'-oxygen of dApdT) — independently confirmed by the orchestrator. All 46 answer keys pass referential integrity, 0 accessibility leaks, 0 hints containing their own answer. **One chemical blocker, corrected:** both strand-directionality questions named DNA residues as ribonucleosides ('the adenosine residue' in a strand containing thymine) and one distractor placed a 2'-oxygen on a DNA residue that cannot have one — contradicting the chapter's own practice check. Verified errors corrected same-day (18 change groups, 41 edits): DNA residues renamed to their 2'-deoxy compounds with the impossible distractor replaced by a possible direction-reversal claim; **seven `wikipedia_title` values carried raw spaces so 7 of 12 compiled external links were malformed** (found by the orchestrator integrity check, not a persona — now 12/12 return 200); **this was the only package in the repo using markdown headings, which `RichText` does not implement, so the longest section rendered literal '###'** (converted to bold lead-ins); six captions stopped instructing readers to locate positions the render does not label (RDKit indices are 0-based traversal order, not chemical locants — labelling would have misled); two stems stopped promising structures they do not show; melting temperature, pyrophosphorolysis and dichloroacetic acid taught where they are graded; N-methylimidazole dropped from a card since it appears nowhere in the chapter; thymidine naming exception moved into the prose; beta-attack feedback corrected to deliver a diphosphate; depurination ratio softened from an overstated 100× to the qualitative claim; S_N2→SN2; capping prompt/answer-set reconciled; why_optional now declares concepts 9+10 a single removable unit and two missing prerequisite edges added; textbook note stopped claiming a one-to-one OpenStax map and names 28.5/28.8 as out of scope. **NEW this chapter: the `clipart` asset type and image-block attribution footer** (previously only external_link/mcmurry_link rendered any credit), used for two public-domain NIAID DNA/RNA figures. Post-correction estimate **blocked** — both remaining blockers are the platform accessibility tickets (hotspot atoms named 'P atom 16' with no chemical mapping; structure_scaffold has no non-drawing path — same as ch1/11/15-19/23/25/26); **setting those aside, estimated ready-with-minor**. **One persona blocker was rejected on evidence:** the visual persona called the four deferred video blocks dead media affordances, but `is_hidden` is honoured at [internal source reference — not in this repo] before the switch and TopicPackageChapterRenderer delegates to it — verified independently, retained as a low-severity flag-fragility note. **The figure cluster all four personas found has since been drawn with RDKit and wired in:** A·T, G·C and the failed A·C pair each in pairing orientation with every hydrogen bond dashed and every donor/acceptor role written in words; both substitutions at phosphorus as arrow-pushing schemes with curved arrows anchored to real atoms; and the authored synthesis_roadmap spec rendered so it draws instead of compiling to an empty container — **every image block in the chapter now resolves, 9 of 9**. This added a `diagram` asset type (generated figure + required image_url, generated provenance — distinct from `clipart`, which is reproduced art needing credit). Each figure was visually inspected as a raster before wiring in, which caught three defects a schema check cannot: mojibake from RDKit's ISO-8859-1 header, captions colliding with the rings, and an `atomLabel` that had overwritten adenine's exocyclic amino group. Open: only rec-015, the Haworth alpha/beta pair. Not seeded; available:false. | 2026-07-30 |
| 29 | `organic-chemistry-of-metabolic-pathways` | ✅ Reviewed | blocked | Verified errors corrected (21 changes: all 6 chemistry blockers plus the reader long-description delivery gap); post-correction estimate **major revision**; `access-005` (no non-visual input path for structure drawing) still open, so a full regression review is due | 2026-07-30 |
| 30 | `orbitals-and-pericyclic-reactions` | ✅ Reviewed | major revision | Produced and reviewed 2026-07-30/31. **Chemistry is the strongest in the series and survived two independent machine audits**: every SMILES and InChIKey verified (7-dehydrocholesterol, previtamin D3, cholecalciferol, chorismate, (1s,4s)-prephenate), the cyclopentadiene/maleic anhydride adduct confirmed genuinely **endo by 3D embedding** rather than merely labelled, all four canonical electrocyclic results correct with their meso/racemic analysis, and all 22 answer keys right. Accessibility scored it **8.3 and the most accessible chapter in this family**: all 36 assets carry alt_text plus a long_description, all 11 surfaced questions are answerable from text alone, all 66 hints are text or worked_step, every concept keeps a keyboard-complete item, both drawing items allow typed entry with a gradeable key, and 0 of 22 accessible descriptions leak. **One blocker, corrected:** the reader defaults to the `expanded` text tier and this chapter had authored expanded as commentary ON standard rather than a superset OF it, so a student on default settings was never given conrotatory, disrotatory, HOMO, LUMO, suprafacial, antarafacial, the [i,j] rule, the 4n/4n+2 statement, or the definition of an electrocyclic reaction — section 5 opened by advising against memorising a table the student had never seen. Fixed by making expanded a superset in all ten nuggets (verified: 12/12 load-bearing definitions now in the default tier, 10/10 supersets). **A production self-inflicted contradiction was also caught:** the 4π reaction direction had been corrected in the prose to the ring OPENING that cyclobutene strain actually permits, but that fix was never grepped into the question bank, so two items still asserted a thermal 4π closure — the same class of miss as ch28's. 14 correction groups applied in all: the unifying **Woodward-Hoffmann statement the chapter promised in section 1 and never delivered** (with the disrotatory=suprafacial / conrotatory=antarafacial bridge), three unkept forward references to a table the platform has no block type for, a false OpenStax coverage claim (30.9 is not a concept), an **invalid `barrier: "high"` that silently coerced to medium** while three descriptions called it high, a figure description that contradicted its own committed SVG, the [i,j]-count-follows-the-array rule worked on the ring case, a 'boat' distractor that penalised students who had read the chapter's only Cope figure (which is genuinely a boat), five descriptions claiming to show things their figures do not, an answer-adjacent numeric placeholder, and typed-entry notation plus manual-review instructor notes on both drawing items. Post-correction estimate **major revision** — blocker resolved, but ~12 high findings remain: **three personas independently found that the object every prediction is read off (the terminal lobes of butadiene ψ2 / hexatriene ψ3) is depicted nowhere**, and the visual persona established this needs no new orbital SVG since the `diagram` type already exists and is unused here; conrotatory/disrotatory motion, endo/exo, the chair [3,3] TS, the [2+2] counterexample and the s-cis gate are all undrawn (all five video briefs deferred and compiling hidden); and **30 authored trouble spots plus 10 worked practice_checks reach no student at all** — a compiler/reader gap, not an authoring omission. Platform tickets: stereo mismatch escalates to manual review with no score; typed-structure read-back cannot express stereochemistry; `showImages: false` deletes figures **together with their text equivalents** (32 of 36 assets); the energy diagram's long_description is dropped by ReactionCoordinateCard; orbital phase is hue-only though every manifest declares +/- labels; Unicode subscripts unconverted in five question renderers; h2→h4 heading gap. One disagreement retained unresolved (Visual wants the duplicate figure cards pruned, Accessibility's logic protects them as independent text equivalents). Not seeded; available:false. **SECOND CORRECTION PASS 2026-07-31 — now SEEDED + LIVE (available:true).** Closed the top open items: built `fig-polyene-frontier-lobes` (butadiene ψ2 / hexatriene ψ3 with every lobe sign printed as a +/- glyph as well as coloured, internodes drawn, lobe heights proportional to the coefficient, terminal lobes ringed) and `fig-electrocyclic-rotation-modes`, both built **deterministically from Hückel coefficients** in `[internal source reference — not in this repo]` with the node count and terminal relationship ASSERTED against the chapter — a generative image model was deliberately not used, because this figure's entire content is exact sign and node placement. **Three platform gaps fixed:** the reader now emits the authored scaffolding as callouts (all 30 trouble spots as warnings, all 10 practice checks as tips — previously they reached NO student); `showImages: false` now converts a suppressed figure into an info callout carrying its long description instead of deleting the text equivalent with the picture (was losing 32 of 38 figures' descriptions); and `TypedStructureRenderer` now requests `sections: ["stereochemistry"]` so the typed read-back **discriminates the answer key from the graded distractor** (it returned byte-identical text before) — fixed in the caller, not by redefining the intro tier, whose no-stereo-prose contract its own tests enforce. **6 new surfaced questions + 6 variants** (22→34 entries, 11→17 surfaced, core items 2→5) closing sigmatropic electron counting, the [1,5]-vs-[1,3] contrast, Claisen product prediction, the ethylene π/π* phase pattern (text-only, no image stimulus), and a core first-contact rung on both concepts that previously opened at advanced. Seeded 17 published / 17 staged (owner 1), re-run reports all unchanged. **Still open:** stereo mismatch escalates to manual_review with no score (platform default deliberately not changed — it would alter grading on every already-seeded drawing question); the orbital library's four SVGs remain hue-only phase and `scientific_review` not `verified`; ReactionCoordinateCard still drops long_description; subscripts unconverted in five question renderers; endo/exo, [2+2] and s-cis still undrawn. No science review record written — the reviewers were AI personas, so the compiler's available-without-review warning is expected and matches every other live chapter. Post-correction estimate **ready with minor revisions**. | 2026-07-31 |
| 31 | `synthetic-polymers` | ✅ Reviewed | blocked | Produced, seeded and reviewed 2026-07-30/31 — the first chapter reviewed with its bank already live in Supabase (13 published + 13 staged variants, owner 1). **Chemistry re-verified independently twice**: all 43 asset SMILES and every question structure parse with correct formulas, correct Z for cis-1,4-polyisoprene and E for trans, (S,S) L-lactide and all-(S) PLLA, balanced reaction SMILES, and every number in the chapter and on all five figures correct (Carothers at 99%/98%, both Mw problems, the Mn/Mw/Ð practice check, the 2%/5% stoichiometric-imbalance caps). **Two chemical blockers, corrected:** the polyoctenamer repeat unit was given seven saturated carbons where ring-opening cyclooctene (C₈H₁₄) gives six, and `mol-polynorbornene-segment` both depicted one ring where its title claimed two **and** misassigned ROMP's atom fate — its description said the six-membered ring becomes the backbone and a new five-membered ring closes, when cleaving the C=C computationally shows a five-membered ring survives *intact* and nothing closes. **Two delivery blockers, corrected:** the reader defaults to `expanded` and *replaces* rather than appends, and all ten expanded tiers were authored as supplements, so at default depth the chapter never defined isotactic, never named the four copolymer architectures and never stated the Carothers relationship (same class as ch30 and ch23; fixed by prepending a self-contained definitional lead-in to all ten); and `ch31-match-linkage-to-polymer` labelled its four stimuli **'Model A'–'Model D'** — the standing ch27/ch15 rule against opaque option labels — with the chemistry living in `structure_smiles`. **NEW platform finding: neither `MatchingRenderer` nor `SelectedResponseRenderer` reads `structure_smiles` at all (only `imageUrl`), so no option structure renders for any learner in these types**; worked around by making labels self-describing, gap unfixed. 18 correction groups applied in all, including the prerequisite graph (copolymers omitted living polymerisation, properties omitted step growth, metathesis declared an edge it never used), the reason isotactic chains are not optically active (achirality by chain symmetry, not cancelling rotations), a phosgene/polycarbonate item testing a reaction the chapter never taught (replaced with glycolic acid), four figure descriptions that omitted numbers baked into the SVGs, and three figure regenerations — the chain-vs-step plateau striking through its own table heading, an illustrative '98.9% crossing' printed to the same precision as the exact Carothers values (removed), a colour-only curve key (now dashed/solid, greyscale verified), a copolymer caption contradicted by its own statistics, and an atactic row drawn 4-up/1-down that scanned as 'isotactic with one defect' (now 2-up/3-down, dyads r m r r recomputed and asserted from the drawn sides). **One persona blocker rejected on evidence:** the visual persona called the six deferred video blocks dead 'Watch' links; block-level `is_hidden` is true and `[internal source reference — not in this repo]accessible_description`s reach no student surface) along with the figure cluster (metallacyclobutane, bracket-and-subscript-n notation, block-copolymer domain morphology) and a lightly assessed concluding third (no Tg/Tm, dispersity or degradation item, none requiring Mn). Bank reseeded after corrections (2 published items updated, verified in Supabase). available:false. **SECOND CORRECTION PASS 2026-07-31** — closed both remaining figure recommendations and the reader half of the practice-check delivery gap. Built `fig-metallacyclobutane` (three panels tracking carbons a/b/c from carbene + hex-3-ene into the four-membered ring and out along *each* of its two cleavages: the two bonds just made break and the reagents come back, the two inherited bonds break and pent-2-ene leaves with a new carbene on the metal — the ring, the cuts and the released fragments all derived in `[internal source reference — not in this repo]fig-repeat-unit-notation` (monomer → consumed bond → brackets cutting the two open valences with subscript n, the bracketed monomer struck through beside the bracketed repeat unit, and propylene written out so the methyl on every second backbone carbon is visible; both same-formula claims counted off RDKit and both bracket positions asserted to fall strictly inside the open-bond stubs). **The metathesis section had carried no diagram of any kind and section 1 had never shown the notation its first objective asks a student to produce.** Third change was free: **this chapter predated the ch30 platform fix that emits `practice_check`/`trouble_spots` as reader callouts, so a recompile alone took the block inventory from 0 scaffolding blocks to 20 callouts** — all ten practice checks and all thirty trouble spots now reach a student. Image blocks 6 → 8, all resolving. 147 tests pass; the five pre-existing SVGs regenerate byte-identically. Both new figures were rasterised and visually inspected before wiring in. **Still open:** `accessible_description` reaches none of this chapter's eleven question types (only two renderers plus the demo page read it — re-verified by grep; closing it means changing `ActivityWorkspaceRenderer` for every seeded chapter, so it needs sign-off), `structure_smiles` renders in neither `MatchingRenderer` nor `SelectedResponseRenderer`, the concluding third is still lightly assessed, three cluster figures (block-copolymer domain morphology, evolving step-growth population, closing two-axis organiser) and the polycarbonate reaction asset. Not reseeded — no question changed in this pass. | 2026-07-31 |

## Counting rules

- Count a chapter when both `chapter-review.md` and `chapter-review.json` exist
 and the four persona envelopes validate.
- Keep the baseline verdict even after corrections; record the corrected state
 separately unless a full four-persona regression run produces a new verdict.
- Select “next” as the lowest-numbered eligible unreviewed package in the
 catalog. Skip catalog chapters that do not yet have a topic package and
 compiled reader/question artifacts.
